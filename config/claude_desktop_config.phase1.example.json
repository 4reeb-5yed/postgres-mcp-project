{
  "mcpServers": {
    "postgres-mcp": {
      "command": "uvx",
      "args": [
        "--with",
        "mcp<2.0.0",
        "postgres-mcp",
        "postgresql://postgres:<YOUR_PASSWORD>@localhost:5433/postgres"
      ]
    }
  }
}
